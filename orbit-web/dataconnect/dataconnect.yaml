specVersion: "v1beta"
serviceId: "orbit-web"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "orbit-web-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
